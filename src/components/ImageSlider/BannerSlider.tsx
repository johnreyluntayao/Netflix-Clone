import { Text, View } from "react-native";
import { ImageSlider } from "react-native-image-slider-banner";

interface SliderProps {
  image: string[];
}

const BannerSlider = () => {

  return (
    <View className="w-full h-[590px]">
      

    </View>
  );
};

export default BannerSlider;
